import {ControlledCarousel} from "../UI/ControlledCarousel"

export const Banner = () => {
    return (
        <div className={'banner'} id={'banner'}>
            <ControlledCarousel/>
        </div>
    )
}